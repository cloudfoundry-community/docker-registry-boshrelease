set -e

PIP_USE_MIRRORS=true

cd docker-registry
mkdir vendor
sudo pip install --install-option="--home=$(pwd)/vendor" setuptools
sudo pip install --install-option="--home=$(pwd)/vendor" -r requirements.txt

# Add this into jobs
export PYTHONPATH=$(pwd)/vendor/lib/python
