---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: docker_registry1
  security_groups:
    - docker_registry

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: docker-registry
    version: latest

properties: {}
